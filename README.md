# 🔍 Мониторинг конкурентов — AI Ассистент

MVP приложение для анализа конкурентной среды с поддержкой текста и изображений.

![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o-purple.svg)

## 📋 Описание

Приложение позволяет:
- **Анализировать текст конкурентов** — структурированная аналитика: сильные/слабые стороны, уникальные предложения и рекомендации
- **Анализировать изображения** — баннеры, скриншоты сайтов, упаковки товаров с оценкой визуального стиля и маркетинговыми инсайтами
- **Парсить сайты** — извлечение title, H1, первого абзаца и автоматический анализ через AI (с скриншотом в Chrome)
- **Хранить историю** — последние 10 запросов в файле `history.json`

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
# Перейдите в папку проекта
cd <путь-к-проекту>

# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
# Windows (cmd):
venv\Scripts\activate
# Windows (PowerShell):
.\venv\Scripts\Activate.ps1
# Linux / macOS:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта. Скопируйте шаблон из `env.example.txt`:

```bash
# Windows (cmd):
copy env.example.txt .env
# Windows (PowerShell) / Linux / macOS:
cp env.example.txt .env
```

Отредактируйте `.env` и укажите **один** из вариантов API:
- **OPENAI_API_KEY** — прямой API OpenAI ([platform.openai.com](https://platform.openai.com/api-keys))
- **PROXY_API_KEY** — ProxyAPI для РФ ([proxyapi.ru](https://proxyapi.ru/))

Пример минимальной настройки:

```env
# Вариант 1: OpenAI (приоритет)
OPENAI_API_KEY=sk-...

# Вариант 2: ProxyAPI (если OPENAI_API_KEY не задан)
# PROXY_API_KEY=your_key

# Модели (по умолчанию gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini
```

Опционально: `API_HOST`, `API_PORT` — см. `env.example.txt`.

### 3. Запуск приложения

**Способ 1 — через скрипт (рекомендуется):**

```bash
python run.py
```

**Способ 2 — через uvicorn:**

```bash
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

После запуска:
- **Веб-интерфейс:** http://localhost:8000  
- **Swagger UI:** http://localhost:8000/docs  
- **ReDoc:** http://localhost:8000/redoc  

## 📁 Структура проекта

```
<корень проекта>/
├── backend/
│   ├── main.py              # FastAPI-приложение и эндпоинты
│   ├── config.py            # Конфигурация (Settings)
│   ├── models/
│   │   └── schemas.py       # Pydantic-модели запросов/ответов
│   └── services/
│       ├── openai_service.py    # Работа с OpenAI/ProxyAPI (текст, изображения)
│       ├── parser_service.py    # Парсинг и скриншоты через Selenium/Chrome
│       └── history_service.py   # История запросов (history.json)
├── frontend/
│   ├── index.html           # Одностраничный веб-интерфейс
│   ├── styles.css           # Стили
│   └── app.js               # Логика (анализ текста/изображений, парсинг, история)
├── desktop/                 # Десктоп-приложение на PyQt6
│   ├── main.py              # Окно приложения
│   ├── api_client.py        # HTTP-клиент к backend
│   ├── styles.py            # Тема оформления
│   └── requirements.txt     # Зависимости для desktop (PyQt6, requests)
├── run.py                   # Скрипт запуска сервера с выводом ссылок
├── requirements.txt         # Зависимости backend
├── env.example.txt          # Шаблон .env
├── history.json             # Файл истории (создаётся автоматически)
├── README.md
└── docs.md                  # Подробное описание API
```

## 🔧 Функциональность

### Анализ текста (`POST /analyze_text`)
- Вход: JSON с полем `text` (минимум 10 символов).
- Выход: сильные и слабые стороны, уникальные предложения, рекомендации, резюме.

### Анализ изображений (`POST /analyze_image`)
- Вход: файл изображения (PNG, JPG, GIF, WEBP).
- Выход: описание изображения, оценка визуального стиля (0–10), маркетинговые инсайты, рекомендации.
- **Ограничения:** допустим только рабочий контент (скриншоты сайтов, баннеры, упаковка). При недопустимом контенте API возвращает ошибку с сообщением о политике контента.

### Парсинг сайтов (`POST /parse_demo`)
- Вход: JSON с полем `url`.
- Действия: открытие URL в headless Chrome, извлечение title, H1, первого абзаца, создание скриншота; анализ через Vision API (или по тексту, если скриншот недоступен).
- Требуется установленный **Google Chrome** и автоматическая установка ChromeDriver через `webdriver-manager`.

### История (`GET /history`, `DELETE /history`)
- Последние 10 запросов с типом, кратким описанием и временем.

## 🖥️ Десктоп-приложение (PyQt6)

В папке `desktop/` — клиент с графическим интерфейсом для тех же операций (анализ текста, изображений, парсинг). Backend должен быть запущен.

```bash
cd desktop
pip install -r requirements.txt
python main.py
```

Подробнее — в `desktop/README.md`.

## 🛠️ Технологии

- **Backend:** FastAPI, Python 3.9+
- **AI:** OpenAI API (GPT-4o-mini / GPT-4o) или ProxyAPI
- **Frontend:** Vanilla JS, CSS3
- **Парсинг:** Selenium, Chrome/ChromeDriver (webdriver-manager)
- **Валидация и настройки:** Pydantic, pydantic-settings, python-dotenv

## ⚠️ Требования

- Python 3.9+
- API-ключ: OpenAI или ProxyAPI с доступом к текстовой и vision-модели (например, gpt-4o-mini)
- Для парсинга сайтов: установленный **Google Chrome**
- Интернет для запросов к AI и загрузки страниц

## 📖 Документация API

- Интерактивная: http://localhost:8000/docs (Swagger), http://localhost:8000/redoc (ReDoc)
- Текстовая: [docs.md](docs.md)

## 🔒 Политика контента (изображения)

Анализ изображений предназначен для рабочего контента: скриншоты сайтов, рекламные баннеры, фото упаковки. Если загружено изображение, нарушающее политику контента API (например, неприемлемый контент), сервер вернёт ошибку с сообщением вида: *«Недопустимый контент. Изображение не может быть проанализировано из-за ограничений политики контента.»* Пустые результаты в таком случае заменены на явное сообщение об ошибке.

## 📦 Работа с Git

### Клонирование репозитория

```bash
git clone https://github.com/<владелец>/<репозиторий>.git
cd <репозиторий>
```

### Сохранение изменений и отправка на GitHub

```bash
# Посмотреть статус (изменённые файлы)
git status

# Добавить все изменения в индекс
git add .

# Или добавить только конкретные файлы
# git add README.md backend/main.py

# Создать коммит с сообщением
git commit -m "Описание изменений"

# Отправить в удалённый репозиторий (ветка master)
git push origin master
```

### Первая публикация проекта в свой GitHub

1. Создайте новый репозиторий на [github.com/new](https://github.com/new) (не добавляйте README, .gitignore и лицензию при создании).
2. В папке проекта выполните:

```bash
# Добавить ваш репозиторий как remote (подставьте свой логин и имя репозитория)
git remote add origin https://github.com/<ваш-логин>/<имя-репозитория>.git

# Если remote origin уже есть и указывает на другой репозиторий — замените URL:
# git remote set-url origin https://github.com/<ваш-логин>/<имя-репозитория>.git

# Закоммитить изменения и отправить
git add .
git commit -m "Initial commit"
git push -u origin master
```

При `git push` по HTTPS GitHub запросит логин и **Personal Access Token** (пароль аккаунта не подойдёт). Создать токен: [GitHub → Settings → Developer settings → Personal access tokens](https://github.com/settings/tokens).

### Полезные команды

| Команда | Описание |
|--------|----------|
| `git status` | Показать изменённые и неотслеживаемые файлы |
| `git diff` | Показать различия в файлах |
| `git log --oneline` | Краткая история коммитов |
| `git pull origin master` | Скачать и слить изменения с удалённого репозитория |
| `git branch` | Список веток |
| `git checkout -b <ветка>` | Создать и переключиться на новую ветку |

## 📝 Лицензия

MIT License
